[project]
name = "orai"
version = "0.1.0"
description = "Агентная система на базе PocketFlow"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pocketflow>=0.0.1",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0"
]
