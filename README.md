# Orai — Агентная система на базе PocketFlow

Orai — это агентная система для обработки запросов пользователей с помощью языковых моделей. Построена на минималистичном фреймворке [PocketFlow](https://github.com/The-Pocket/PocketFlow).

## 🚀 Возможности

- **Интеллектуальная обработка запросов** — анализ и решение задач через LLM
- **Прозрачность** — полная история обработки каждого запроса
- **Модульная архитектура** — последовательная цепочка специализированных узлов

## 📋 Требования

- Python 3.12+
- OpenAI API ключ

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <url-репозитория>
cd Orai
```

2. Активируйте виртуальное окружение:
```bash
source .venv/bin/activate
```

3. Установите зависимости:
```bash
uv pip install -r requirements.txt
```

4. Настройте OpenAI API:
```bash
cp .env.example .env
```

Откройте `.env` и добавьте ваш API ключ:
```
OPENAI_API_KEY=sk-ваш-ключ-здесь
```

Получите ключ на: https://platform.openai.com/api-keys

## 🎯 Использование

Запустите систему:
```bash
python main.py
```

Введите ваш вопрос или задачу. Система обработает запрос через последовательность этапов и вернёт результат.

### Доступные модели

По умолчанию используется `gpt-4.1-mini`. Для смены модели отредактируйте `.env`:

```
OPENAI_MODEL=gpt-4.1-nano
# или
OPENAI_MODEL=gpt-4.1-mini
```

### Дополнительные настройки

Опциональные переменные в `.env`:

| Переменная | Описание |
|------------|----------|
| `OPENAI_API_KEY` | Ваш API ключ (обязательно) |
| `OPENAI_BASE_URL` | URL API (для прокси или альтернативных эндпоинтов) |
| `OPENAI_MODEL` | Модель по умолчанию (gpt-4.1-mini) |

## 🏗️ Как это работает

Система обрабатывает запросы через цепочку узлов:

```
Ваш запрос → Анализ → Решение → Действие → Результат
```

1. **Получение** — чтение входных данных
2. **Анализ** — понимание задачи через LLM
3. **Решение** — выбор подхода
4. **Действие** — выполнение
5. **Вывод** — форматирование результата

## 🔗 Полезные ссылки

- [OpenAI Platform](https://platform.openai.com/)
- [PocketFlow на GitHub](https://github.com/The-Pocket/PocketFlow)

---

**Powered by PocketFlow**
