# Orai — Агентная система на базе PocketFlow

Orai — это демонстрационная агентная система, построенная на минималистичном LLM-фреймворке [PocketFlow](https://github.com/The-Pocket/PocketFlow). Проект демонстрирует архитектуру последовательного агентного потока для обработки запросов пользователей через цепочку специализированных узлов.

## 🚀 Возможности

- **Минималистичная архитектура**: Всего одна зависимость — PocketFlow
- **Модульная структура**: Каждый узел отвечает за свою задачу
- **Прозрачность**: Полная история обработки запроса
- **Расширяемость**: Легко добавлять новые узлы и возможности
- **Русскоязычная кодовая база**: Все комментарии на русском языке

## 📋 Требования

- Python 3.12+
- uv (для управления зависимостями)

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <url-репозитория>
cd Orai
```

2. Активируйте виртуальное окружение:
```bash
source .venv/bin/activate
```

3. Установите зависимости:
```bash
uv pip install -r requirements.txt
```

## 🎯 Использование

Запустите агентную систему:
```bash
python main.py
```

Система попросит вас ввести вопрос или задачу, затем обработает запрос через последовательность узлов:

1. **InputNode** — получение входных данных
2. **AnalyzeNode** — анализ запроса через LLM
3. **DecisionNode** — принятие решения о действиях
4. **ActionNode** — выполнение действия
5. **OutputNode** — форматирование и вывод результата

## 🏗️ Архитектура

### Основные компоненты

```
orai/
├── main.py              # Точка входа приложения
├── nodes.py             # Определения узлов
├── flow.py              # Создание потоков
├── utils/               # Утилиты
│   └── call_llm.py      # LLM интеграция (mock)
├── docs/                # Документация
│   └── design.md        # Детальный дизайн
└── requirements.txt     # Зависимости
```

### Поток данных

```
[Пользователь]
      ↓
  InputNode ──→ Получение запроса
      ↓
 AnalyzeNode ──→ Анализ через LLM
      ↓
DecisionNode ──→ Принятие решения
      ↓
 ActionNode ──→ Выполнение действия
      ↓
 OutputNode ──→ Вывод результата
      ↓
  [Результат]
```

### PocketFlow: основные концепции

- **Node (Узел)**: Базовая единица логики с методами `prep()`, `exec()`, `post()`
- **Flow (Поток)**: Соединение узлов через оператор `>>`
- **Shared Store**: Общее хранилище для обмена данными между узлами

## 🔌 Интеграция с LLM

Текущая версия использует **mock-реализацию** LLM для демонстрации архитектуры.

### Интеграция с реальным LLM

Для работы с реальными языковыми моделями:

1. Выберите провайдера (OpenAI, Anthropic, Ollama)
2. Установите соответствующую библиотеку:
   ```bash
   uv pip install openai  # или anthropic
   ```
3. Обновите `utils/call_llm.py`
4. Добавьте API ключ в переменные окружения

Пример интеграции с OpenAI:

```python
from openai import OpenAI

client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

def call_llm(prompt, model="gpt-4", temperature=0.7):
    response = client.chat.completions.create(
        model=model,
        messages=[{"role": "user", "content": prompt}],
        temperature=temperature
    )
    return response.choices[0].message.content
```

## 📚 Документация

- **[CLAUDE.md](CLAUDE.md)** — руководство для Claude Code при работе с проектом
- **[docs/design.md](docs/design.md)** — детальное описание архитектуры и дизайна

## 🛠️ Разработка

### Добавление нового узла

1. Создайте класс в `nodes.py`:

```python
from pocketflow import Node

class МойУзел(Node):
    def prep(self, shared):
        # Извлечение данных из shared store
        return shared.get("данные")

    def exec(self, prep_res):
        # Основная логика
        result = обработать(prep_res)
        return result

    def post(self, shared, prep_res, exec_res):
        # Сохранение результатов
        shared["результат"] = exec_res
        return "default"
```

2. Обновите поток в `flow.py`:

```python
мой_узел = МойУзел()
input_node >> analyze_node >> мой_узел >> output_node
```

### Agentic Coding: процесс разработки

Проект следует 8-шаговому процессу Agentic Coding:

1. **Requirements** — оценка применимости AI
2. **Flow Design** — проектирование потока
3. **Utilities** — создание утилит
4. **Data Design** — структура данных
5. **Node Design** — дизайн узлов
6. **Implementation** — реализация
7. **Optimization** — оптимизация промптов
8. **Reliability** — добавление надёжности

## 📝 Соглашения о коде

**ВАЖНО**: Все комментарии в коде должны быть написаны на русском языке.

## 🔗 Полезные ссылки

- [PocketFlow на GitHub](https://github.com/The-Pocket/PocketFlow)
- [PocketFlow Template Python](https://github.com/The-Pocket/PocketFlow-Template-Python)
- [Документация PocketFlow](https://the-pocket.github.io/PocketFlow/)

## 📄 Лицензия

Проект создан в демонстрационных целях.

## 🤝 Контрибуция

Проект открыт для улучшений и расширений. Добро пожаловать в разработку!

---

**Powered by PocketFlow** — минималистичный LLM-фреймворк в 100 строк кода
